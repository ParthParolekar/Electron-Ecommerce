import React from "react";

const FilterButtons = () => {
  return (
    <div className="filter-btns">
      <button className="btn abcd btn-primary">Apply</button>
      <button className="btn btn-primary">Cancel</button>
    </div>
  );
};

export default FilterButtons;
